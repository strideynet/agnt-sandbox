apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: devoops

resources:
  - deployment.yaml
  - service.yaml
  - ssh-config.yaml

secretGenerator:
  - name: devoops-config
    envs:
      - secret.env
    options:
      disableNameSuffixHash: true
