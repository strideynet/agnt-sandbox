apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: devoops

resources:
  - deployment.yaml
  - service.yaml

secretGenerator:
  - name: devoops-ui-config
    envs:
      - secret.env
    options:
      disableNameSuffixHash: true
